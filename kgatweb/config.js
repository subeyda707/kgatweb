// THE ONE SETTING THAT CONNECTS TO YOUR BACKEND.
// Set this to whatever URL your backend is running at -- Render, a Colab
// proxy/tunnel, anything that serves the /summary, /records, /conflicts,
// /chat endpoints. Leave blank to run on realistic sample data only.
export const API_BASE_URL = "";
